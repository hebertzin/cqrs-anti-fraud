# Development overrides — used automatically by `docker compose up`
version: "3.9"

services:
  api:
    build:
      target: builder
    environment:
      LOG_LEVEL: "debug"
    volumes:
      - .:/app
    command: ["go", "run", "./cmd/api"]
