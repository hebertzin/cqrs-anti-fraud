// Package main is the root entry point placeholder.
// The actual service entrypoints are located in:
//   - cmd/api/main.go  — HTTP API server
//
// Run with: go run ./cmd/api
package main

func main() {
	// See cmd/api/main.go for the service entrypoint.
}
